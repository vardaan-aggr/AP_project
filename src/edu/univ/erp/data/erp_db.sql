Create database IF NOT EXIST erp_db;
use erp_db;
Create table  IF NOT EXIST grades {
    students varchar(255),
    instructor role (255),
    courses varchar(255),
};
students, instructors, courses, sections,
enrollments, grades, settings)